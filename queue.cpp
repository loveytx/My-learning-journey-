#include<iostream>
#include<string.h>
#include<queue>
using namespace std;


int main(){
    queue<string> q;
    q.push('A');
    q.push('B');
    q.push('C');
    q.push('D');
    // cout<<"The size of queue is  "<<q.size();
    // // cout<<"The front element is "<<q.front()<<" and rear element is "<<q.back();
    // cout<<"Inserting one element \n";
    return 0;
}